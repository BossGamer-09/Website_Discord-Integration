from django.apps import AppConfig


class MailclientConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "app.mailclient"
    verbose_name = "Mail Client — Staff Inbox"
